# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftModelGenerator' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftModelGenerator
	pod 'SwiftyJSON', '~> 4.0'
  target 'SwiftModelGeneratorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftModelGeneratorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
